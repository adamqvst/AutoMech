//initializing AudioContext
//window.AudioContext = window.AudioContext || window.webkitAudioContext;
//const audioContext = new AudioContext();
//let buffer = null;

//fetches audio from source
//const waveform = source => {
//    fetch(source)
//        .then(response => response.arrayBuffer()) //returns arraybuffer
//        .then(arrayBuffer => audioContext.decodeAudioData(arrayBuffer)) //returns audiobuffer for reading audio
//        .then(audioBuffer => visualize(audioBuffer)); //sends audiobuffer to the filtering(?) and visualization parts of the code
//};

//TODO: filter data(done in backend?), visualize data (JS canvas)


